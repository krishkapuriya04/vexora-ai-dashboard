/**
 * VEXORA API configuration.
 * Override before loading app scripts: window.VEXORA_API_BASE = 'http://localhost:5000';
 */
if (typeof window !== 'undefined' && !window.VEXORA_API_BASE) {
  window.VEXORA_API_BASE = '';
}
